{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Kill dev servers on 8000 3000 3001",
			"type": "shell",
			"command": "$ports = @(8000,3000,3001); $pids = Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue | Where-Object { $ports -contains $_.LocalPort } | Select-Object -ExpandProperty OwningProcess -Unique; foreach ($processId in $pids) { try { Stop-Process -Id $processId -Force -ErrorAction Stop } catch {} }; Write-Output ('Killed PIDs: ' + ($pids -join ', '))",
			"isBackground": false,
			"problemMatcher": [],
			"group": "build"
		},
		{
			"label": "Start backend dev server",
			"type": "shell",
			"command": "Set-Location \"${workspaceFolder}/backend\"; & \"${workspaceFolder}/.venv/Scripts/Activate.ps1\"; python -m uvicorn main:app --port 8010",
			"isBackground": true,
			"problemMatcher": [],
			"group": "build"
		},
			"label": "Kill dev servers on 8010 3000 3001",
			"label": "Start frontend dev server",
			"command": "$ports = @(8010,3000,3001); $pids = Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue | Where-Object { $ports -contains $_.LocalPort } | Select-Object -ExpandProperty OwningProcess -Unique; foreach ($processId in $pids) { try { Stop-Process -Id $processId -Force -ErrorAction Stop } catch {} }; Write-Output ('Killed PIDs: ' + ($pids -join ', '))",
			"command": "Set-Location \"${workspaceFolder}/frontend\"; npm run dev",
			"isBackground": true,
			"problemMatcher": [],
			"group": "build"
		}
	]
}